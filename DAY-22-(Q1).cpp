class Solution {
public:
    int countNegatives(vector<vector<int>>& grid) {
        
        //Brute Force
        int cnt=0; 
        for(int i=0;i,grid.size();i++){
            int s=0, e=grid[i].size()-1;
            while(s<=e){
                int mid = s+(e-s)/2;
                
            }
        } 
    }
};
